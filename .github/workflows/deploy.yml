# Deploy is handled by iconsjo1/endedge-vps → Actions → "Deploy Website".
# The Contabo self-hosted runner (vmi1166319) is registered on that repo.

name: Deploy Website

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Run the Deploy Website workflow in iconsjo1/endedge-vps instead."
          echo "https://github.com/iconsjo1/endedge-vps/actions/workflows/deploy-website.yml"
